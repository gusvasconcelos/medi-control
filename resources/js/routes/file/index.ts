import userMedications from './user-medications'
import users from './users'

const file = {
    userMedications: Object.assign(userMedications, userMedications),
    users: Object.assign(users, users),
}

export default file